[build-system]
build-backend = "setuptools.build_meta"
# use same cmake version as vcpkg (2023.02.24) for pep-517 builds
requires = ["setuptools>=40.1.0", "wheel", "cmake==3.21.1", "pybind11==2.10.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
